{
  "devDependencies": {
    "esbuild": "^0.23.1",
    "kubernetes-types": "1.22.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "dev": "esbuild src/nodes.ts --outdir=public/dist --bundle --sourcemap --servedir=public",
    "build": "esbuild src/nodes.ts --outdir=public/dist --bundle --sourcemap"
  }
}
